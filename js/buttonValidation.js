/* 
TODO: Fix fancy alert required when done
let checker = () => {
    let checkName = document.forms.event.callerName.value;
    //let checkName = document.getElementById("callerName").value;
    let evtTarget = document.getElementById("event");
    if (checkName === null || checkName === undefined || checkName === "") {
        evtTarget.preventDefault();
        alert("Caller's name is required.");
        return;
    }
    else {
        console.log(checkName)
    }
    return;
} */